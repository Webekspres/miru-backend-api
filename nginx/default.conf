upstream django {
    server web:8000;
}

server {
    listen 80;
    server_name _;

    # Redirect HTTP → HTTPS (hanya aktif saat SSL tersedia)
    # return 301 https://$host$request_uri;

    client_max_body_size 20M;

    # Static files (collectstatic)
    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Media files (upload) — KTP & file privat di volume lokal
    location /media/ {
        alias /app/media/;
        expires 7d;
        add_header Cache-Control "public";
    }

    # Object publik (artikel edukasi) — proxy ke bucket MinIO
    location /objects/ {
        proxy_pass http://minio:9000/mirubanksampah/;
        proxy_set_header Host minio:9000;
        proxy_hide_header x-amz-id-2;
        proxy_hide_header x-amz-request-id;
        expires 7d;
        add_header Cache-Control "public";
    }

    # API & Admin
    location / {
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }
}

# Server block untuk HTTPS (aktifkan saat SSL sudah tersedia)
# server {
#     listen 443 ssl;
#     server_name api.mirubanksampah.id;
#
#     ssl_certificate     /etc/ssl/certs/fullchain.pem;
#     ssl_certificate_key /etc/ssl/private/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     client_max_body_size 20M;
#
#     location /static/ {
#         alias /app/staticfiles/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /media/ {
#         alias /app/media/;
#         expires 7d;
#         add_header Cache-Control "public";
#     }
#
#     location / {
#         proxy_pass http://django;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_redirect off;
#     }
# }
